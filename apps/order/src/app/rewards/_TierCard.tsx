"use client";

/**
 * Tier card on /rewards — same component as the account screen so the
 * tier visual stays consistent across the app. Implementation lives in
 * /components/TierCard.tsx (port of apps/pickup-native/components
 * /TierCard.tsx).
 */
export { TierCard } from "@/components/TierCard";
