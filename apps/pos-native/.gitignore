# Expo / RN
node_modules/
.expo/
dist/
web-build/
*.log

# Native build artifacts (regenerated by prebuild / EAS)
/android
/ios

# Env (anon key is public, but keep local env out of git anyway)
.env
.env.local

# macOS
.DS_Store
